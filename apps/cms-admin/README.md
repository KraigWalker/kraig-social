# CMS Admin

React Router admin UI for CMS posts and versions.

## Environment

- `CMS_API_BASE_URL` (required, e.g. `http://localhost:3335`)
- `CMS_API_TOKEN` (optional bearer token for server-side requests)

## Local commands

- `rushx dev`
